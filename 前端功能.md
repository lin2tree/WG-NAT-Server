# 前端功能文档

本文档记录 FCloudVPN 管理前端的所有功能页面和操作说明。

## 页面列表

### 1. 登录页面 (LoginView)

**路径**: `/login`

**功能描述**:
- 管理员登录入口
- 支持用户名密码登录
- 密码使用 RSA 公钥加密传输
- 支持回车键快捷登录
- 登录成功后跳转到 VPN 配置页面

**操作流程**:
1. 输入用户名
2. 输入密码
3. 点击"登录"按钮或按回车键
4. 系统验证身份后跳转到主页面

**安全特性**:
- 密码在前端使用 RSA-OAEP 加密后传输
- 认证失败只提示"登录失败"，不暴露具体原因
- 缺少用户名或密码时提示"请输入完整的用户名和密码"

**权限要求**: 无需登录

**API 调用**:
- `GET /api/auth/public-key` - 获取 RSA 公钥
- `POST /api/auth/login/encrypted` - 加密登录

---

### 2. VPN 配置页面 (VpnConfigView)

**路径**: `/configs`

**功能描述**:
- 查看所有 VPN 配置列表
- 查看配置详细信息
- 下载服务端配置文件 (仅 Root)
- 导出配置为 CSV (仅 Root)
- 查看配置状态 (init/started)

**表格字段**:
| 字段 | 说明 |
|-----|------|
| VM IP | 虚拟机内部 IP 地址 |
| VM Port | WireGuard 服务端口 |
| Pub IP | 公网 IP 地址 |
| Pub Port | 公网端口 |
| 状态 | init/started |
| 初始化时间 | 配置创建时间 |
| 启动时间 | 配置启动时间 |

**搜索功能**:
- 按状态筛选 (init/started)
- 按 VM IP 搜索（需为资源池中的 IP）
- 按 Pub Port 搜索（范围 1-65535）
- 输入验证：非资源池 IP 或非法端口会提示错误

**导出功能** (Root 管理员):
- 支持导出筛选结果为 CSV
- 使用 UTF-8-BOM 编码，Excel 可正常打开

**权限要求**: 需要登录

**API 调用**:
- `GET /api/admin/configs` - 获取配置列表
- `GET /api/admin/configs/export` - 导出 CSV
- `GET /api/admin/configs/{vm_ip}/download/server` - 下载服务端配置

**角色权限**:
- Root 管理员: 可查看、可下载、可导出
- 普通管理员: 仅可查看

---

### 3. 已归档数据页面 (ArchiveView)

**路径**: `/archives`

**功能描述**:
- 查看已删除的 VPN 配置归档
- 导出归档数据为 CSV (仅 Root)

**表格字段**:
| 字段 | 说明 |
|-----|------|
| VM IP | 虚拟机内部 IP 地址 |
| VM Port | WireGuard 服务端口 |
| Pub IP | 公网 IP 地址 |
| Pub Port | 公网端口 |
| 状态 | deleted |
| 初始化时间 | 配置创建时间 |
| 启动时间 | 配置启动时间 |
| 删除时间 | 配置删除时间 |

**搜索功能**:
- 按 VM IP 搜索（需为资源池中的 IP）
- 按 Pub Port 搜索（范围 1-65535）
- 输入验证：非资源池 IP 或非法端口会提示错误

**权限要求**: 需要登录

**API 调用**:
- `GET /api/admin/archives` - 获取归档列表
- `GET /api/admin/archives/export` - 导出 CSV

---

### 4. 资源池管理页面 (ResourcePoolView)

**路径**: `/resource-pool`

**功能描述**:
- 查看端口范围配置
- 设置端口范围 (仅 Root)
- 查看资源池映射列表
- 导入 IP 地址 (仅 Root) - 支持多种格式
- 删除单个映射 (仅 Root)
- 批量删除映射 (仅 Root)
- 导出映射为 CSV
- 搜索功能

**搜索功能**:
- 按内网 IP 搜索（模糊匹配）
- 按公网端口搜索（精确匹配）

**删除规则**:
- 只能删除"有配置"列为"否"的资源
- 存在活跃配置的 IP 无法删除，会提示用户
- 批量删除时会自动跳过有配置的记录

**IP 导入格式**:
| 格式 | 示例 | 说明 |
|-----|------|------|
| 单个 IP | `192.168.1.100` | 单个 IP 地址 |
| IP 范围 | `192.168.1.100-192.168.1.110` | 从起始到结束的所有 IP |
| CIDR 格式 | `192.168.1.0/24` | 指定网段的所有可用 IP |

**导入规则**:
- 每行一个输入项
- 重复 IP 自动去重
- 已存在的 IP 自动跳过
- 系统自动分配端口

**权限要求**: 需要登录

**API 调用**:
- `GET /api/admin/port-range` - 获取端口范围
- `POST /api/admin/port-range` - 设置端口范围
- `GET /api/admin/resource-pool` - 获取资源池列表（支持搜索参数）
- `POST /api/admin/resource-pool/import` - 导入 IP 地址
- `DELETE /api/admin/resource-pool/{id}` - 删除单个映射
- `POST /api/admin/resource-pool/batch-delete` - 批量删除映射
- `GET /api/admin/resource-pool/export` - 导出 CSV

**角色权限**:
- Root 管理员: 完全控制权限
- 普通管理员: 仅查看权限

---

### 5. 用户管理页面 (UserManagementView)

**路径**: `/users`

**功能描述**:
- 查看所有用户列表 (仅 Root)
- 创建新用户 (仅 Root)
- 删除用户 (仅 Root)
- 修改用户密码 (仅 Root)
- 修改自己的密码 (所有用户)

**用户角色**:
- Root: 超级管理员，拥有所有权限
- Admin: 普通管理员，仅可查看

**修改密码流程**:
1. Root 用户修改他人密码：直接输入新密码
2. 普通用户修改自己密码：需验证原密码

**权限要求**: 需要登录

**API 调用**:
- `GET /api/auth/users` - 获取用户列表
- `POST /api/auth/users` - 创建用户
- `DELETE /api/auth/users/{id}` - 删除用户
- `PUT /api/auth/users/{id}/password` - 修改用户密码
- `PUT /api/auth/me/password` - 修改自己密码

---

### 6. 操作日志页面 (LogView)

**路径**: `/logs`

**功能描述**:
- 查看所有操作日志
- 按时间范围筛选
- 按 IP 地址筛选
- 按请求路径筛选
- 查看请求详情

**权限要求**: 需要登录

**API 调用**:
- `GET /api/admin/logs` - 获取日志列表

---

## 通用功能

### 导航菜单

- **VPN 配置**: 跳转到 VPN 配置页面
- **已归档数据**: 跳转到归档数据页面
- **资源池**: 跳转到资源池管理页面 (仅 Root 可见)
- **用户管理**: 跳转到用户管理页面
- **操作日志**: 跳转到操作日志页面
- **退出登录**: 清除登录状态，返回登录页面

### 用户信息显示

- 页面右上角显示当前登录用户
- 显示用户角色 (Root/Admin)

### 权限控制

- 未登录用户自动跳转到登录页面
- 普通管理员无法访问 Root 专属功能
- 无权限操作时显示提示信息

---

## 状态码说明

| 状态 | 说明 |
|-----|------|
| init | 配置已创建，等待虚拟机就绪 |
| started | 虚拟机已就绪，配置可用 |
| deleted | 配置已删除，已归档 |

---

## 安全特性

### 密码传输加密
- 登录密码使用 RSA-OAEP 公钥加密
- 服务器使用私钥解密验证
- 网络传输中密码不可见

### 输入验证
- VM IP 搜索：验证是否在资源池中
- Pub Port 搜索：验证端口范围 1-65535
- 密码修改：验证原密码（普通用户）

### 错误提示
- 认证失败：不暴露具体原因
- 权限不足：明确提示权限要求
- 输入错误：明确提示错误原因

---

## 更新记录

| 日期 | 版本 | 更新内容 |
|-----|------|---------|
| 2026-04-17 | 1.0.0 | 初始版本，包含所有核心功能 |
| 2026-04-19 | 1.1.0 | 新增：RSA 加密登录、用户管理、已归档数据、CSV 导出、多种 IP 导入格式、输入验证 |
