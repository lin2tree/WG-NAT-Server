# 前端功能文档

本文档记录 FCloudVPN 管理前端的所有功能页面和操作说明。

## 页面列表

### 1. 登录页面 (LoginView)

**路径**: `/login`

**功能描述**:
- 管理员登录入口
- 支持用户名密码登录
- 登录成功后跳转到 VPN 配置页面

**操作流程**:
1. 输入用户名
2. 输入密码
3. 点击"登录"按钮
4. 系统验证身份后跳转到主页面

**权限要求**: 无需登录

**API 调用**:
- `POST /api/auth/login` - 用户登录

---

### 2. VPN 配置页面 (VpnConfigView)

**路径**: `/vpn-configs`

**功能描述**:
- 查看所有 VPN 配置列表
- 查看配置详细信息
- 下载服务端配置文件 (仅 Root)
- 下载客户端配置文件 (仅 Root)
- 查看配置状态 (init/started)

**操作流程**:

#### 查看配置列表
1. 页面自动加载所有配置
2. 支持按 IP 地址搜索
3. 支持按状态筛选
4. 支持分页浏览

#### 查看配置详情
1. 点击配置行的"详情"按钮
2. 弹窗显示完整配置信息
3. 显示服务端公钥、私钥
4. 显示所有客户端配置

#### 下载配置文件 (Root 管理员)
1. 点击"下载服务端配置"按钮
2. 下载 WireGuard 配置文件
3. 或点击"下载客户端配置"按钮
4. 选择要下载的客户端编号

**权限要求**: 需要登录

**API 调用**:
- `GET /api/admin/configs` - 获取配置列表
- `GET /api/admin/configs/{vm_ip}` - 获取配置详情
- `GET /api/admin/configs/{vm_ip}/download/server` - 下载服务端配置
- `GET /api/admin/configs/{vm_ip}/download/client/{client_id}` - 下载客户端配置

**角色权限**:
- Root 管理员: 可查看、可下载
- 普通管理员: 仅可查看

---

### 3. 资源池管理页面 (ResourcePoolView)

**路径**: `/resource-pool`

**功能描述**:
- 查看端口范围配置
- 设置端口范围 (仅 Root)
- 查看资源池映射列表
- 导入 IP 地址 (仅 Root)
- 删除映射 (仅 Root)
- 导出映射为 CSV

**操作流程**:

#### 查看端口范围
1. 页面顶部显示当前端口范围配置
2. 显示起始端口和结束端口

#### 设置端口范围 (Root 管理员)
1. 点击"设置端口范围"按钮
2. 输入起始端口 (如 10000)
3. 输入结束端口 (如 20000)
4. 点击"确定"保存

#### 查看资源池列表
1. 页面显示所有 IP-端口映射
2. 显示内部 IP 地址
3. 显示分配的公网端口
4. 支持分页浏览

#### 导入 IP 地址 (Root 管理员)
1. 点击"导入 IP"按钮
2. 输入 IP 地址列表 (每行一个)
3. 或上传 CSV 文件
4. 点击"导入"确认
5. 系统自动分配端口

#### 删除映射 (Root 管理员)
1. 选择要删除的映射行
2. 点击"删除"按钮
3. 确认删除操作

#### 导出映射
1. 点击"导出 CSV"按钮
2. 下载包含所有映射的 CSV 文件

**权限要求**: 需要登录

**API 调用**:
- `GET /api/admin/port-range` - 获取端口范围
- `POST /api/admin/port-range` - 设置端口范围
- `GET /api/admin/resource-pool` - 获取资源池列表
- `POST /api/admin/resource-pool/import` - 导入 IP 地址
- `DELETE /api/admin/resource-pool/{id}` - 删除映射
- `GET /api/admin/resource-pool/export` - 导出 CSV

**角色权限**:
- Root 管理员: 完全控制权限
- 普通管理员: 仅查看权限

---

### 4. 操作日志页面 (LogView)

**路径**: `/logs`

**功能描述**:
- 查看所有操作日志
- 按时间范围筛选
- 按 IP 地址筛选
- 按请求路径筛选
- 查看请求详情

**操作流程**:

#### 查看日志列表
1. 页面自动加载最近的日志
2. 默认显示最近 100 条记录
3. 支持分页浏览

#### 筛选日志
1. 选择时间范围 (开始时间、结束时间)
2. 输入源 IP 地址
3. 输入请求路径关键字
4. 点击"搜索"按钮
5. 显示符合条件的日志

#### 查看日志详情
1. 点击日志行的"详情"按钮
2. 弹窗显示完整请求信息
3. 显示请求时间、方法、状态码
4. 显示响应时间 (毫秒)

**权限要求**: 需要登录

**API 调用**:
- `GET /api/admin/logs` - 获取日志列表

**角色权限**:
- Root 管理员: 可查看
- 普通管理员: 可查看

---

## 通用功能

### 导航菜单

- **VPN 配置**: 跳转到 VPN 配置页面
- **资源池**: 跳转到资源池管理页面
- **操作日志**: 跳转到操作日志页面
- **退出登录**: 清除登录状态，返回登录页面

### 用户信息显示

- 页面右上角显示当前登录用户
- 显示用户角色 (Root/管理员)

### 权限控制

- 未登录用户自动跳转到登录页面
- 普通管理员无法访问 Root 专属功能
- 无权限操作时显示提示信息

---

## 状态码说明

| 状态 | 说明 |
|-----|------|
| init | 配置已创建，等待虚拟机就绪 |
| started | 虚拟机已就绪，配置可用 |

---

## 错误处理

### 登录失败
- 用户名或密码错误: 提示"用户名或密码错误"
- 网络错误: 提示"网络连接失败，请稍后重试"

### 操作失败
- 权限不足: 提示"权限不足，无法执行此操作"
- 数据不存在: 提示"数据不存在或已被删除"
- 网络错误: 提示"操作失败，请稍后重试"

---

## 浏览器兼容性

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

## 更新记录

| 日期 | 版本 | 更新内容 |
|-----|------|---------|
| 2026-04-17 | 1.0.0 | 初始版本，包含所有核心功能 |
